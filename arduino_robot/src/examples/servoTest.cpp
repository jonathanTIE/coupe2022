#include "servoTest.h"
#include "../params.h"

void servoTest::update() {


    /*
        if(TestTime.check())
    {
      MotorControl::set_cons(0.f,sp[i]);
      i = (i+1) % 4;
    }  */
    /*
		if (TestTime.check())
    {
		  mot2=(mot2+20)%255;
    }
    */
}
